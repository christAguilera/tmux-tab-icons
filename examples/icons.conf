# <POSIX extended regex> <glyph>
# `:<text>:` in a window name is replaced by the glyph when <text> matches the regex entirely.

n?vim 
zsh|bash|fish 
node 
python3? 
git 
docker 
ssh 󰣀
claude 󰺹
